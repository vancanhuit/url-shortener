/* tslint:disable */
/* eslint-disable */
/**
 * url-shortener API
 * The url-shortener service exposes a small JSON HTTP API plus a public 302 redirect endpoint. This document describes both, along with the operational endpoints (`/livez`, `/readyz`, `/version`) used by orchestrators and humans.  ## Conventions  - All JSON request/response bodies use `application/json` with   UTF-8 encoding. - Timestamps are RFC 3339 (`2006-01-02T15:04:05.999999999Z`). - Error responses share a single shape (`ErrorResponse`) with a   stable machine-readable `code` field for client-side branching;   the human-readable `error` field is for surfacing in UIs. - Rate limiting and authentication are out of scope today; the   service trusts its callers and is expected to run behind a   reverse proxy that enforces both.  ## Idempotency notes  - `POST /api/v1/links` is idempotent on the *normalized*   `target_url`: a second call returns the existing row with   `200 OK` instead of minting a fresh code (only for permanent,   auto-generated requests; user-supplied codes and expiring   links opt out). - `DELETE /api/v1/links/{code}` is semantically idempotent (the   row stays deleted) but response-distinct: the first call   returns `204`, subsequent calls return `404`. Clients that need   `204+204` semantics can collapse the two themselves. 
 *
 * The version of the OpenAPI document: 1.0.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import { mapValues } from '../runtime';
/**
 * 
 * @export
 * @interface LinkResponse
 */
export interface LinkResponse {
    /**
     * 
     * @type {string}
     * @memberof LinkResponse
     */
    code: string;
    /**
     * The fully-qualified short URL (`{base_url}/r/{code}`).
     * @type {string}
     * @memberof LinkResponse
     */
    short_url: string;
    /**
     * 
     * @type {string}
     * @memberof LinkResponse
     */
    target_url: string;
    /**
     * 
     * @type {Date}
     * @memberof LinkResponse
     */
    created_at: Date;
    /**
     * Lifetime hit count for `/r/{code}`. Eventually-consistent:
     * increments are applied fire-and-forget so a freshly-served
     * redirect may not be reflected in the next response.
     * 
     * @type {number}
     * @memberof LinkResponse
     */
    click_count: number;
    /**
     * RFC 3339 expiry. Omitted entirely (rather than rendered
     * as `null`) for permanent links, so a one-key check
     * distinguishes them.
     * 
     * @type {Date}
     * @memberof LinkResponse
     */
    expires_at?: Date;
}

/**
 * Check if a given object implements the LinkResponse interface.
 */
export function instanceOfLinkResponse(value: object): value is LinkResponse {
    if (!('code' in value) || value['code'] === undefined) return false;
    if (!('short_url' in value) || value['short_url'] === undefined) return false;
    if (!('target_url' in value) || value['target_url'] === undefined) return false;
    if (!('created_at' in value) || value['created_at'] === undefined) return false;
    if (!('click_count' in value) || value['click_count'] === undefined) return false;
    return true;
}

export function LinkResponseFromJSON(json: any): LinkResponse {
    return LinkResponseFromJSONTyped(json, false);
}

export function LinkResponseFromJSONTyped(json: any, ignoreDiscriminator: boolean): LinkResponse {
    if (json == null) {
        return json;
    }
    return {
        
        'code': json['code'],
        'short_url': json['short_url'],
        'target_url': json['target_url'],
        'created_at': (new Date(json['created_at'])),
        'click_count': json['click_count'],
        'expires_at': json['expires_at'] == null ? undefined : (new Date(json['expires_at'])),
    };
}

export function LinkResponseToJSON(json: any): LinkResponse {
    return LinkResponseToJSONTyped(json, false);
}

export function LinkResponseToJSONTyped(value?: LinkResponse | null, ignoreDiscriminator: boolean = false): any {
    if (value == null) {
        return value;
    }

    return {
        
        'code': value['code'],
        'short_url': value['short_url'],
        'target_url': value['target_url'],
        'created_at': value['created_at'].toISOString(),
        'click_count': value['click_count'],
        'expires_at': value['expires_at'] == null ? value['expires_at'] : value['expires_at'].toISOString(),
    };
}

