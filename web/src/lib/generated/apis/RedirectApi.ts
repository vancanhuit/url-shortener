/* tslint:disable */
/* eslint-disable */
/**
 * url-shortener API
 * The url-shortener service exposes a small JSON HTTP API plus a public 302 redirect endpoint. This document describes both, along with the operational endpoints (`/livez`, `/readyz`, `/version`) used by orchestrators and humans.  ## Conventions  - All JSON request/response bodies use `application/json` with   UTF-8 encoding. - Timestamps are RFC 3339 (`2006-01-02T15:04:05.999999999Z`). - Error responses share a single shape (`ErrorResponse`) with a   stable machine-readable `code` field for client-side branching;   the human-readable `error` field is for surfacing in UIs. - Rate limiting and authentication are out of scope today; the   service trusts its callers and is expected to run behind a   reverse proxy that enforces both.  ## Idempotency notes  - `POST /api/v1/links` is idempotent on the *normalized*   `target_url`: a second call returns the existing row with   `200 OK` instead of minting a fresh code (only for permanent,   auto-generated requests; user-supplied codes and expiring   links opt out). - `DELETE /api/v1/links/{code}` is semantically idempotent (the   row stays deleted) but response-distinct: the first call   returns `204`, subsequent calls return `404`. Clients that need   `204+204` semantics can collapse the two themselves. 
 *
 * The version of the OpenAPI document: 1.0.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import * as runtime from '../runtime';

export interface RedirectLinkRequest {
    code: string;
}

/**
 * RedirectApi - interface
 * 
 * @export
 * @interface RedirectApiInterface
 */
export interface RedirectApiInterface {
    /**
     * Creates request options for redirectLink without sending the request
     * @param {string} code The short code identifying the link. Must match &#x60;^[0-9A-Za-z]{4,64}$&#x60;; codes that fail this shape check are rejected with &#x60;404&#x60; rather than &#x60;422&#x60; so the API doesn\&#39;t leak its validation rules through the path parameter. 
     * @throws {RequiredError}
     * @memberof RedirectApiInterface
     */
    redirectLinkRequestOpts(requestParameters: RedirectLinkRequest): Promise<runtime.RequestOpts>;

    /**
     * The public short-URL endpoint. On a hit, returns `302 Found` with `Location: <target_url>` and fires a fire-and-forget click-counter increment that never delays the response. Hot path is served from a Redis read-through cache; the cache TTL is clamped to the link\'s remaining lifetime so an expired link can never be served from cache.  Soft-deleted and expired links return `410 Gone`. The response body is plain text; programmatic clients that need the machine-readable `code` field should call `GET /api/v1/links/{code}` instead. 
     * @summary Redirect to the link\'s target URL.
     * @param {string} code The short code identifying the link. Must match &#x60;^[0-9A-Za-z]{4,64}$&#x60;; codes that fail this shape check are rejected with &#x60;404&#x60; rather than &#x60;422&#x60; so the API doesn\&#39;t leak its validation rules through the path parameter. 
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof RedirectApiInterface
     */
    redirectLinkRaw(requestParameters: RedirectLinkRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<void>>;

    /**
     * The public short-URL endpoint. On a hit, returns `302 Found` with `Location: <target_url>` and fires a fire-and-forget click-counter increment that never delays the response. Hot path is served from a Redis read-through cache; the cache TTL is clamped to the link\'s remaining lifetime so an expired link can never be served from cache.  Soft-deleted and expired links return `410 Gone`. The response body is plain text; programmatic clients that need the machine-readable `code` field should call `GET /api/v1/links/{code}` instead. 
     * Redirect to the link\'s target URL.
     */
    redirectLink(requestParameters: RedirectLinkRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<void>;

}

/**
 * 
 */
export class RedirectApi extends runtime.BaseAPI implements RedirectApiInterface {

    /**
     * Creates request options for redirectLink without sending the request
     */
    async redirectLinkRequestOpts(requestParameters: RedirectLinkRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['code'] == null) {
            throw new runtime.RequiredError(
                'code',
                'Required parameter "code" was null or undefined when calling redirectLink().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};


        let urlPath = `/r/{code}`;
        urlPath = urlPath.replace('{code}', encodeURIComponent(String(requestParameters['code'])));

        return {
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        };
    }

    /**
     * The public short-URL endpoint. On a hit, returns `302 Found` with `Location: <target_url>` and fires a fire-and-forget click-counter increment that never delays the response. Hot path is served from a Redis read-through cache; the cache TTL is clamped to the link\'s remaining lifetime so an expired link can never be served from cache.  Soft-deleted and expired links return `410 Gone`. The response body is plain text; programmatic clients that need the machine-readable `code` field should call `GET /api/v1/links/{code}` instead. 
     * Redirect to the link\'s target URL.
     */
    async redirectLinkRaw(requestParameters: RedirectLinkRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<void>> {
        const requestOptions = await this.redirectLinkRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.VoidApiResponse(response);
    }

    /**
     * The public short-URL endpoint. On a hit, returns `302 Found` with `Location: <target_url>` and fires a fire-and-forget click-counter increment that never delays the response. Hot path is served from a Redis read-through cache; the cache TTL is clamped to the link\'s remaining lifetime so an expired link can never be served from cache.  Soft-deleted and expired links return `410 Gone`. The response body is plain text; programmatic clients that need the machine-readable `code` field should call `GET /api/v1/links/{code}` instead. 
     * Redirect to the link\'s target URL.
     */
    async redirectLink(requestParameters: RedirectLinkRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<void> {
        await this.redirectLinkRaw(requestParameters, initOverrides);
    }

}
