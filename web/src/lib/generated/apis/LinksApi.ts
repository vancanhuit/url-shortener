/* tslint:disable */
/* eslint-disable */
/**
 * url-shortener API
 * The url-shortener service exposes a small JSON HTTP API plus a public 302 redirect endpoint. This document describes both, along with the operational endpoints (`/livez`, `/readyz`, `/version`) used by orchestrators and humans.  ## Conventions  - All JSON request/response bodies use `application/json` with   UTF-8 encoding. - Timestamps are RFC 3339 (`2006-01-02T15:04:05.999999999Z`). - Error responses share a single shape (`ErrorResponse`) with a   stable machine-readable `code` field for client-side branching;   the human-readable `error` field is for surfacing in UIs. - Rate limiting and authentication are out of scope today; the   service trusts its callers and is expected to run behind a   reverse proxy that enforces both.  ## Idempotency notes  - `POST /api/v1/links` is idempotent on the *normalized*   `target_url`: a second call returns the existing row with   `200 OK` instead of minting a fresh code (only for permanent,   auto-generated requests; user-supplied codes and expiring   links opt out). - `DELETE /api/v1/links/{code}` is semantically idempotent (the   row stays deleted) but response-distinct: the first call   returns `204`, subsequent calls return `404`. Clients that need   `204+204` semantics can collapse the two themselves. 
 *
 * The version of the OpenAPI document: 1.0.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import * as runtime from '../runtime';
import {
    type ErrorResponse,
    ErrorResponseFromJSON,
    ErrorResponseToJSON,
} from '../models/ErrorResponse';
import {
    type LinkRequest,
    LinkRequestFromJSON,
    LinkRequestToJSON,
} from '../models/LinkRequest';
import {
    type LinkResponse,
    LinkResponseFromJSON,
    LinkResponseToJSON,
} from '../models/LinkResponse';
import {
    type ListResponse,
    ListResponseFromJSON,
    ListResponseToJSON,
} from '../models/ListResponse';

export interface CreateLinkRequest {
    linkRequest: LinkRequest;
}

export interface DeleteLinkRequest {
    code: string;
}

export interface GetLinkRequest {
    code: string;
}

export interface ListLinksRequest {
    limit?: number;
    before?: number;
}

/**
 * LinksApi - interface
 * 
 * @export
 * @interface LinksApiInterface
 */
export interface LinksApiInterface {
    /**
     * Creates request options for createLink without sending the request
     * @param {LinkRequest} linkRequest 
     * @throws {RequiredError}
     * @memberof LinksApiInterface
     */
    createLinkRequestOpts(requestParameters: CreateLinkRequest): Promise<runtime.RequestOpts>;

    /**
     * Creates a new short link for the supplied `target_url`. If `code` is omitted, the server generates a 7-character base62 code; if supplied, it must match `^[0-9A-Za-z]{4,64}$`.  When `code` is omitted *and* `expires_at` is omitted, the request is deduped against the existing rows: a previously shortened permanent link with the same normalized target URL is returned with `200 OK` instead of a fresh `201 Created` row. Callers requesting a custom code or an expiring link always opt out of dedup. 
     * @summary Create a short link.
     * @param {LinkRequest} linkRequest 
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof LinksApiInterface
     */
    createLinkRaw(requestParameters: CreateLinkRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<LinkResponse>>;

    /**
     * Creates a new short link for the supplied `target_url`. If `code` is omitted, the server generates a 7-character base62 code; if supplied, it must match `^[0-9A-Za-z]{4,64}$`.  When `code` is omitted *and* `expires_at` is omitted, the request is deduped against the existing rows: a previously shortened permanent link with the same normalized target URL is returned with `200 OK` instead of a fresh `201 Created` row. Callers requesting a custom code or an expiring link always opt out of dedup. 
     * Create a short link.
     */
    createLink(requestParameters: CreateLinkRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<LinkResponse>;

    /**
     * Creates request options for deleteLink without sending the request
     * @param {string} code The short code identifying the link. Must match &#x60;^[0-9A-Za-z]{4,64}$&#x60;; codes that fail this shape check are rejected with &#x60;404&#x60; rather than &#x60;422&#x60; so the API doesn\&#39;t leak its validation rules through the path parameter. 
     * @throws {RequiredError}
     * @memberof LinksApiInterface
     */
    deleteLinkRequestOpts(requestParameters: DeleteLinkRequest): Promise<runtime.RequestOpts>;

    /**
     * Marks the link as deleted by stamping an internal `deleted_at` tombstone. The row stays in storage so audit columns (`created_at`, `click_count`) survive, but subsequent redirect, lookup, list, and dedup operations stop seeing it. The cache entry, if any, is invalidated server-side so the next `GET /r/{code}` returns `410` immediately rather than waiting out the cache TTL.  Semantically idempotent (the row stays deleted) but response-distinct: the first DELETE returns `204`, the second returns `404`. Clients that need `204+204` can collapse the two themselves. 
     * @summary Soft-delete a link.
     * @param {string} code The short code identifying the link. Must match &#x60;^[0-9A-Za-z]{4,64}$&#x60;; codes that fail this shape check are rejected with &#x60;404&#x60; rather than &#x60;422&#x60; so the API doesn\&#39;t leak its validation rules through the path parameter. 
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof LinksApiInterface
     */
    deleteLinkRaw(requestParameters: DeleteLinkRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<void>>;

    /**
     * Marks the link as deleted by stamping an internal `deleted_at` tombstone. The row stays in storage so audit columns (`created_at`, `click_count`) survive, but subsequent redirect, lookup, list, and dedup operations stop seeing it. The cache entry, if any, is invalidated server-side so the next `GET /r/{code}` returns `410` immediately rather than waiting out the cache TTL.  Semantically idempotent (the row stays deleted) but response-distinct: the first DELETE returns `204`, the second returns `404`. Clients that need `204+204` can collapse the two themselves. 
     * Soft-delete a link.
     */
    deleteLink(requestParameters: DeleteLinkRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<void>;

    /**
     * Creates request options for getLink without sending the request
     * @param {string} code The short code identifying the link. Must match &#x60;^[0-9A-Za-z]{4,64}$&#x60;; codes that fail this shape check are rejected with &#x60;404&#x60; rather than &#x60;422&#x60; so the API doesn\&#39;t leak its validation rules through the path parameter. 
     * @throws {RequiredError}
     * @memberof LinksApiInterface
     */
    getLinkRequestOpts(requestParameters: GetLinkRequest): Promise<runtime.RequestOpts>;

    /**
     * Returns the metadata for the link without performing a redirect or incrementing the click counter. Soft-deleted and expired links surface as `410 Gone` with a distinct `code` (`link_deleted` vs `link_expired`) so programmatic clients can distinguish a once-valid code from one that never existed (`404`). 
     * @summary Fetch link metadata.
     * @param {string} code The short code identifying the link. Must match &#x60;^[0-9A-Za-z]{4,64}$&#x60;; codes that fail this shape check are rejected with &#x60;404&#x60; rather than &#x60;422&#x60; so the API doesn\&#39;t leak its validation rules through the path parameter. 
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof LinksApiInterface
     */
    getLinkRaw(requestParameters: GetLinkRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<LinkResponse>>;

    /**
     * Returns the metadata for the link without performing a redirect or incrementing the click counter. Soft-deleted and expired links surface as `410 Gone` with a distinct `code` (`link_deleted` vs `link_expired`) so programmatic clients can distinguish a once-valid code from one that never existed (`404`). 
     * Fetch link metadata.
     */
    getLink(requestParameters: GetLinkRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<LinkResponse>;

    /**
     * Creates request options for listLinks without sending the request
     * @param {number} [limit] Page size. Defaults to 10. Values above 100 are silently clamped down. Non-positive or non-numeric values fall back to the default. 
     * @param {number} [before] Exclusive lower bound on the row id; pass the previous page\&#39;s &#x60;next_cursor&#x60; to walk backwards in time. Omit (or pass &#x60;0&#x60;) for the first page. 
     * @throws {RequiredError}
     * @memberof LinksApiInterface
     */
    listLinksRequestOpts(requestParameters: ListLinksRequest): Promise<runtime.RequestOpts>;

    /**
     * Returns a page of links ordered newest-first (by internal id), suitable for the recent-links view in the web UI. Pagination is cursor-based: pass the previous response\'s `next_cursor` as the `before` query parameter on the next request to walk older rows. `next_cursor` is `null` on the last page.  Soft-deleted and expired rows are excluded server-side, so a busy site that prunes regularly still pages predictably. Bad or out-of-range `limit` values fall back to the default rather than failing the request. 
     * @summary List recent links.
     * @param {number} [limit] Page size. Defaults to 10. Values above 100 are silently clamped down. Non-positive or non-numeric values fall back to the default. 
     * @param {number} [before] Exclusive lower bound on the row id; pass the previous page\&#39;s &#x60;next_cursor&#x60; to walk backwards in time. Omit (or pass &#x60;0&#x60;) for the first page. 
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof LinksApiInterface
     */
    listLinksRaw(requestParameters: ListLinksRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<ListResponse>>;

    /**
     * Returns a page of links ordered newest-first (by internal id), suitable for the recent-links view in the web UI. Pagination is cursor-based: pass the previous response\'s `next_cursor` as the `before` query parameter on the next request to walk older rows. `next_cursor` is `null` on the last page.  Soft-deleted and expired rows are excluded server-side, so a busy site that prunes regularly still pages predictably. Bad or out-of-range `limit` values fall back to the default rather than failing the request. 
     * List recent links.
     */
    listLinks(requestParameters: ListLinksRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<ListResponse>;

}

/**
 * 
 */
export class LinksApi extends runtime.BaseAPI implements LinksApiInterface {

    /**
     * Creates request options for createLink without sending the request
     */
    async createLinkRequestOpts(requestParameters: CreateLinkRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['linkRequest'] == null) {
            throw new runtime.RequiredError(
                'linkRequest',
                'Required parameter "linkRequest" was null or undefined when calling createLink().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';


        let urlPath = `/api/v1/links`;

        return {
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: LinkRequestToJSON(requestParameters['linkRequest']),
        };
    }

    /**
     * Creates a new short link for the supplied `target_url`. If `code` is omitted, the server generates a 7-character base62 code; if supplied, it must match `^[0-9A-Za-z]{4,64}$`.  When `code` is omitted *and* `expires_at` is omitted, the request is deduped against the existing rows: a previously shortened permanent link with the same normalized target URL is returned with `200 OK` instead of a fresh `201 Created` row. Callers requesting a custom code or an expiring link always opt out of dedup. 
     * Create a short link.
     */
    async createLinkRaw(requestParameters: CreateLinkRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<LinkResponse>> {
        const requestOptions = await this.createLinkRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => LinkResponseFromJSON(jsonValue));
    }

    /**
     * Creates a new short link for the supplied `target_url`. If `code` is omitted, the server generates a 7-character base62 code; if supplied, it must match `^[0-9A-Za-z]{4,64}$`.  When `code` is omitted *and* `expires_at` is omitted, the request is deduped against the existing rows: a previously shortened permanent link with the same normalized target URL is returned with `200 OK` instead of a fresh `201 Created` row. Callers requesting a custom code or an expiring link always opt out of dedup. 
     * Create a short link.
     */
    async createLink(requestParameters: CreateLinkRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<LinkResponse> {
        const response = await this.createLinkRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for deleteLink without sending the request
     */
    async deleteLinkRequestOpts(requestParameters: DeleteLinkRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['code'] == null) {
            throw new runtime.RequiredError(
                'code',
                'Required parameter "code" was null or undefined when calling deleteLink().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};


        let urlPath = `/api/v1/links/{code}`;
        urlPath = urlPath.replace('{code}', encodeURIComponent(String(requestParameters['code'])));

        return {
            path: urlPath,
            method: 'DELETE',
            headers: headerParameters,
            query: queryParameters,
        };
    }

    /**
     * Marks the link as deleted by stamping an internal `deleted_at` tombstone. The row stays in storage so audit columns (`created_at`, `click_count`) survive, but subsequent redirect, lookup, list, and dedup operations stop seeing it. The cache entry, if any, is invalidated server-side so the next `GET /r/{code}` returns `410` immediately rather than waiting out the cache TTL.  Semantically idempotent (the row stays deleted) but response-distinct: the first DELETE returns `204`, the second returns `404`. Clients that need `204+204` can collapse the two themselves. 
     * Soft-delete a link.
     */
    async deleteLinkRaw(requestParameters: DeleteLinkRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<void>> {
        const requestOptions = await this.deleteLinkRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.VoidApiResponse(response);
    }

    /**
     * Marks the link as deleted by stamping an internal `deleted_at` tombstone. The row stays in storage so audit columns (`created_at`, `click_count`) survive, but subsequent redirect, lookup, list, and dedup operations stop seeing it. The cache entry, if any, is invalidated server-side so the next `GET /r/{code}` returns `410` immediately rather than waiting out the cache TTL.  Semantically idempotent (the row stays deleted) but response-distinct: the first DELETE returns `204`, the second returns `404`. Clients that need `204+204` can collapse the two themselves. 
     * Soft-delete a link.
     */
    async deleteLink(requestParameters: DeleteLinkRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<void> {
        await this.deleteLinkRaw(requestParameters, initOverrides);
    }

    /**
     * Creates request options for getLink without sending the request
     */
    async getLinkRequestOpts(requestParameters: GetLinkRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['code'] == null) {
            throw new runtime.RequiredError(
                'code',
                'Required parameter "code" was null or undefined when calling getLink().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};


        let urlPath = `/api/v1/links/{code}`;
        urlPath = urlPath.replace('{code}', encodeURIComponent(String(requestParameters['code'])));

        return {
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        };
    }

    /**
     * Returns the metadata for the link without performing a redirect or incrementing the click counter. Soft-deleted and expired links surface as `410 Gone` with a distinct `code` (`link_deleted` vs `link_expired`) so programmatic clients can distinguish a once-valid code from one that never existed (`404`). 
     * Fetch link metadata.
     */
    async getLinkRaw(requestParameters: GetLinkRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<LinkResponse>> {
        const requestOptions = await this.getLinkRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => LinkResponseFromJSON(jsonValue));
    }

    /**
     * Returns the metadata for the link without performing a redirect or incrementing the click counter. Soft-deleted and expired links surface as `410 Gone` with a distinct `code` (`link_deleted` vs `link_expired`) so programmatic clients can distinguish a once-valid code from one that never existed (`404`). 
     * Fetch link metadata.
     */
    async getLink(requestParameters: GetLinkRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<LinkResponse> {
        const response = await this.getLinkRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for listLinks without sending the request
     */
    async listLinksRequestOpts(requestParameters: ListLinksRequest): Promise<runtime.RequestOpts> {
        const queryParameters: any = {};

        if (requestParameters['limit'] != null) {
            queryParameters['limit'] = requestParameters['limit'];
        }

        if (requestParameters['before'] != null) {
            queryParameters['before'] = requestParameters['before'];
        }

        const headerParameters: runtime.HTTPHeaders = {};


        let urlPath = `/api/v1/links`;

        return {
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        };
    }

    /**
     * Returns a page of links ordered newest-first (by internal id), suitable for the recent-links view in the web UI. Pagination is cursor-based: pass the previous response\'s `next_cursor` as the `before` query parameter on the next request to walk older rows. `next_cursor` is `null` on the last page.  Soft-deleted and expired rows are excluded server-side, so a busy site that prunes regularly still pages predictably. Bad or out-of-range `limit` values fall back to the default rather than failing the request. 
     * List recent links.
     */
    async listLinksRaw(requestParameters: ListLinksRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<ListResponse>> {
        const requestOptions = await this.listLinksRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => ListResponseFromJSON(jsonValue));
    }

    /**
     * Returns a page of links ordered newest-first (by internal id), suitable for the recent-links view in the web UI. Pagination is cursor-based: pass the previous response\'s `next_cursor` as the `before` query parameter on the next request to walk older rows. `next_cursor` is `null` on the last page.  Soft-deleted and expired rows are excluded server-side, so a busy site that prunes regularly still pages predictably. Bad or out-of-range `limit` values fall back to the default rather than failing the request. 
     * List recent links.
     */
    async listLinks(requestParameters: ListLinksRequest = {}, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<ListResponse> {
        const response = await this.listLinksRaw(requestParameters, initOverrides);
        return await response.value();
    }

}
