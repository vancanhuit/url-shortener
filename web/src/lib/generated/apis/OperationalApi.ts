/* tslint:disable */
/* eslint-disable */
/**
 * url-shortener API
 * The url-shortener service exposes a small JSON HTTP API plus a public 302 redirect endpoint. This document describes both, along with the operational endpoints (`/livez`, `/readyz`, `/version`) used by orchestrators and humans.  ## Conventions  - All JSON request/response bodies use `application/json` with   UTF-8 encoding. - Timestamps are RFC 3339 (`2006-01-02T15:04:05.999999999Z`). - Error responses share a single shape (`ErrorResponse`) with a   stable machine-readable `code` field for client-side branching;   the human-readable `error` field is for surfacing in UIs. - Rate limiting and authentication are out of scope today; the   service trusts its callers and is expected to run behind a   reverse proxy that enforces both.  ## Idempotency notes  - `POST /api/v1/links` is idempotent on the *normalized*   `target_url`: a second call returns the existing row with   `200 OK` instead of minting a fresh code (only for permanent,   auto-generated requests; user-supplied codes and expiring   links opt out). - `DELETE /api/v1/links/{code}` is semantically idempotent (the   row stays deleted) but response-distinct: the first call   returns `204`, subsequent calls return `404`. Clients that need   `204+204` semantics can collapse the two themselves. 
 *
 * The version of the OpenAPI document: 1.0.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import * as runtime from '../runtime';
import {
    type Livez200Response,
    Livez200ResponseFromJSON,
    Livez200ResponseToJSON,
} from '../models/Livez200Response';
import {
    type ReadyzResponse,
    ReadyzResponseFromJSON,
    ReadyzResponseToJSON,
} from '../models/ReadyzResponse';
import {
    type VersionResponse,
    VersionResponseFromJSON,
    VersionResponseToJSON,
} from '../models/VersionResponse';

/**
 * OperationalApi - interface
 * 
 * @export
 * @interface OperationalApiInterface
 */
export interface OperationalApiInterface {
    /**
     * Creates request options for livez without sending the request
     * @throws {RequiredError}
     * @memberof OperationalApiInterface
     */
    livezRequestOpts(): Promise<runtime.RequestOpts>;

    /**
     * Returns `200` as long as the process is running and the HTTP stack is responsive. Has no dependencies on Postgres or Redis -- a flapping database must not cause Kubernetes to restart the pod. 
     * @summary Liveness probe.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof OperationalApiInterface
     */
    livezRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<Livez200Response>>;

    /**
     * Returns `200` as long as the process is running and the HTTP stack is responsive. Has no dependencies on Postgres or Redis -- a flapping database must not cause Kubernetes to restart the pod. 
     * Liveness probe.
     */
    livez(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<Livez200Response>;

    /**
     * Creates request options for readyz without sending the request
     * @throws {RequiredError}
     * @memberof OperationalApiInterface
     */
    readyzRequestOpts(): Promise<runtime.RequestOpts>;

    /**
     * Pings every registered dependency (Postgres, Redis) with a short per-check timeout and returns `200` only when all succeed. The body lists per-check results so operators can see which dependency is unhappy. Flips to `503` during graceful shutdown so a load balancer drains the pod cleanly before the listener closes. 
     * @summary Readiness probe.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof OperationalApiInterface
     */
    readyzRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<ReadyzResponse>>;

    /**
     * Pings every registered dependency (Postgres, Redis) with a short per-check timeout and returns `200` only when all succeed. The body lists per-check results so operators can see which dependency is unhappy. Flips to `503` during graceful shutdown so a load balancer drains the pod cleanly before the listener closes. 
     * Readiness probe.
     */
    readyz(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<ReadyzResponse>;

    /**
     * Creates request options for version without sending the request
     * @throws {RequiredError}
     * @memberof OperationalApiInterface
     */
    versionRequestOpts(): Promise<runtime.RequestOpts>;

    /**
     * Returns the `version`, `commit`, and `date` baked into the binary at build time via `-ldflags`.
     * @summary Build metadata.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof OperationalApiInterface
     */
    versionRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<VersionResponse>>;

    /**
     * Returns the `version`, `commit`, and `date` baked into the binary at build time via `-ldflags`.
     * Build metadata.
     */
    version(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<VersionResponse>;

}

/**
 * 
 */
export class OperationalApi extends runtime.BaseAPI implements OperationalApiInterface {

    /**
     * Creates request options for livez without sending the request
     */
    async livezRequestOpts(): Promise<runtime.RequestOpts> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};


        let urlPath = `/livez`;

        return {
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        };
    }

    /**
     * Returns `200` as long as the process is running and the HTTP stack is responsive. Has no dependencies on Postgres or Redis -- a flapping database must not cause Kubernetes to restart the pod. 
     * Liveness probe.
     */
    async livezRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<Livez200Response>> {
        const requestOptions = await this.livezRequestOpts();
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => Livez200ResponseFromJSON(jsonValue));
    }

    /**
     * Returns `200` as long as the process is running and the HTTP stack is responsive. Has no dependencies on Postgres or Redis -- a flapping database must not cause Kubernetes to restart the pod. 
     * Liveness probe.
     */
    async livez(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<Livez200Response> {
        const response = await this.livezRaw(initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for readyz without sending the request
     */
    async readyzRequestOpts(): Promise<runtime.RequestOpts> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};


        let urlPath = `/readyz`;

        return {
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        };
    }

    /**
     * Pings every registered dependency (Postgres, Redis) with a short per-check timeout and returns `200` only when all succeed. The body lists per-check results so operators can see which dependency is unhappy. Flips to `503` during graceful shutdown so a load balancer drains the pod cleanly before the listener closes. 
     * Readiness probe.
     */
    async readyzRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<ReadyzResponse>> {
        const requestOptions = await this.readyzRequestOpts();
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => ReadyzResponseFromJSON(jsonValue));
    }

    /**
     * Pings every registered dependency (Postgres, Redis) with a short per-check timeout and returns `200` only when all succeed. The body lists per-check results so operators can see which dependency is unhappy. Flips to `503` during graceful shutdown so a load balancer drains the pod cleanly before the listener closes. 
     * Readiness probe.
     */
    async readyz(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<ReadyzResponse> {
        const response = await this.readyzRaw(initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for version without sending the request
     */
    async versionRequestOpts(): Promise<runtime.RequestOpts> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};


        let urlPath = `/version`;

        return {
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        };
    }

    /**
     * Returns the `version`, `commit`, and `date` baked into the binary at build time via `-ldflags`.
     * Build metadata.
     */
    async versionRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<VersionResponse>> {
        const requestOptions = await this.versionRequestOpts();
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => VersionResponseFromJSON(jsonValue));
    }

    /**
     * Returns the `version`, `commit`, and `date` baked into the binary at build time via `-ldflags`.
     * Build metadata.
     */
    async version(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<VersionResponse> {
        const response = await this.versionRaw(initOverrides);
        return await response.value();
    }

}
