{
  "name": "url-shortener-tooling",
  "version": "0.0.0",
  "private": true,
  "description": "Dev-only tooling (husky + commitlint) for the url-shortener Go project.",
  "scripts": {
    "prepare": "husky"
  },
  "devDependencies": {
    "@commitlint/cli": "^19.5.0",
    "@commitlint/config-conventional": "^19.5.0",
    "husky": "^9.1.6"
  },
  "commitlint": {
    "extends": [
      "@commitlint/config-conventional"
    ]
  }
}
