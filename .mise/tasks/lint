#!/usr/bin/env bash
# [MISE] description="Run linters (auto-installs golangci-lint at the pinned version if missing)."
# [MISE] depends=["lint-install", "web-build", "tidy"]
set -euo pipefail
bindir="${BIN_DIR:-$HOME/.local/bin}"
"$bindir/golangci-lint" run -v --build-tags=integration
