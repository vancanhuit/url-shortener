#!/usr/bin/env bash
# [MISE] description="Build the local Docker image (url-shortener:dev) and scan it with Trivy."
# [MISE] depends=["docker-build"]
set -euo pipefail
trivy image \
    --severity HIGH,CRITICAL \
    --ignore-unfixed \
    --exit-code 1 \
    --no-progress \
    url-shortener:dev
